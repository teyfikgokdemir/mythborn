{
  "name": "mythborn",
  "version": "1.0.0",
  "private": true,
  "description": "Temporary launch page for mythborn.co on Cloudflare Workers",
  "scripts": {
    "dev": "wrangler dev",
    "audit": "node scripts/audit.mjs",
    "deploy": "npm run audit && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}