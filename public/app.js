(()=>{const api=async(path,options={})=>{const response=await fetch(path,{...options,headers:{'content-type':'application/json',...(options.headers||{})}});let data={};try{data=await response.json()}catch{}if(!response.ok)throw new Error(data.error||'İşlem tamamlanamadı.');return data};document.querySelectorAll('[data-auth-form]').forEach(form=>form.addEventListener('submit',async event=>{event.preventDefault();const mode=form.dataset.authForm;const message=form.querySelector('[data-form-message]');const submit=form.querySelector('button[type="submit"]');message.textContent='';submit.disabled=true;try{const values=new FormData(form);await api(mode==='giris'?'/api/auth/login':'/api/auth/register',{method:'POST',body:JSON.stringify({email:values.get('email'),password:values.get('password')})});const next=new URLSearchParams(location.search).get('devam');location.href=next&&next.startsWith('/')?next:'/hesabim'}catch(error){message.textContent=error.message}finally{submit.disabled=false}}));document.querySelector('[data-logout]')?.addEventListener('click',async()=>{try{await api('/api/auth/logout',{method:'POST',body:'{}'})}finally{location.href='/'}});const account=document.querySelector('[data-account]');if(account){(async()=>{try{const session=await api('/api/auth/me');account.querySelector('[data-account-email]').textContent=session.user.email;account.querySelector('[data-account-status]').textContent='Ücretsiz üye · Tüm açılımlar açık';const history=await api('/api/results');const root=account.querySelector('[data-result-history]');root.innerHTML=history.results?.length?history.results.map(item=>`<article class="history-item"><strong>${item.archetype_name}</strong><span>${new Date(item.created_at).toLocaleDateString('tr-TR')}</span></article>`).join(''):'<p>Henüz kaydedilmiş açılımın yok.</p>'}catch{location.href='/giris?devam=/hesabim'}})()}window.MythbornApi=api;})();