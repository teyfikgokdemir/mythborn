import app from './index.js';

const SITE='https://mythborn.co';
const routes=new Set(['/','/deneyim','/uyelik','/giris','/kayit','/hesabim','/arketipler','/manifesto','/hakkinda','/gizlilik','/kvkk','/kullanim-kosullari','/cerezler','/mesafeli-satis','/on-bilgilendirme','/iptal-iade']);
const llms=`# Mythborn\n\n> Mythborn, arzuları oynanabilir seçimlere, arketiplere ve kişisel sonuçlara dönüştüren Türkçe dijital deneyim platformudur.\n\n- Resmî site: ${SITE}/\n- Dil: Türkçe\n- Üyelik: Aylık 115 TL\n- İletişim: info@mythborn.co\n`;
const sitemap=`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${[...routes].filter(x=>!['/giris','/kayit','/hesabim'].includes(x)).map(x=>`<url><loc>${SITE}${x}</loc></url>`).join('')}</urlset>`;
const security=(h=new Headers())=>{h.set('strict-transport-security','max-age=31536000');h.set('x-content-type-options','nosniff');h.set('referrer-policy','strict-origin-when-cross-origin');h.set('permissions-policy','camera=(), microphone=(), geolocation=()');return h};
const text=(body,status,type)=>new Response(body,{status,headers:security(new Headers({'content-type':type,'cache-control':'public, max-age=300'}))});
export default{async fetch(request,env,ctx){const url=new URL(request.url);if(url.hostname==='www.mythborn.co'){url.hostname='mythborn.co';return Response.redirect(url.toString(),301)}if(url.pathname==='/llms.txt')return text(llms,200,'text/plain; charset=utf-8');if(url.pathname==='/robots.txt')return text(`User-agent: *\nAllow: /\nSitemap: ${SITE}/sitemap.xml\n`,200,'text/plain; charset=utf-8');if(url.pathname==='/sitemap.xml')return text(sitemap,200,'application/xml; charset=utf-8');if(url.pathname.startsWith('/images/')||['/app.css','/app.js','/favicon.svg','/consent.js'].includes(url.pathname))return env.ASSETS.fetch(request);if(!routes.has(url.pathname))return text('<!doctype html><html lang="tr"><meta charset="utf-8"><meta name="robots" content="noindex"><title>404 — Mythborn</title><body><h1>Bu kapı henüz açılmadı.</h1><a href="/">Mythborn’a dön</a></body></html>',404,'text/html; charset=utf-8');const response=await app.fetch(request,env,ctx);const headers=security(new Headers(response.headers));headers.set('content-language','tr');return new Response(response.body,{status:response.status,headers})}};